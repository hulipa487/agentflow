version: "1"

# Phase-0 regression example: file-based echo loop, no LLM.
#   curl -X POST localhost:8080/webhook/ -d '{"from":"alice","text":"hi"}'

agents:
  echo:
    loop: ./plugins/examples/echo.lua

gateway:
  # one shared listener; channels mount paths on it
  listen: ":8080"
  channels:
    - type: webhook
      path: /webhook/
      agent: echo
